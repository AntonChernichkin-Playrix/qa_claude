# Обязательные разделы декомпозиции

Каноничный список — задача **«(ХХХ) Шаблон. Декомпозиция фичи со стороны QA»** в Asana (`1205699014619431`, проекты `FD: Templates` и `Disco Team QA`), 38 блоков. Полная выгрузка с исходными описаниями: [decomposition-template-asana.md](decomposition-template-asana.md).

Эти разделы встречаются в каждой фиче. **Заголовок один и тот же, описание проверок — своё под каждую фичу.** Задача декомпозиции не в том, чтобы скопировать блок, а в том, чтобы наполнить его конкретикой из ТЗ.

## Как пользоваться списком

1. Пройти **все** блоки ниже сверху вниз. Ни один не пропускать молча.
2. Обязательные — включить всегда, наполнив спецификой фичи.
3. Условные — включить, если условие выполняется. Если по ТЗ непонятно, выполняется ли, — **это вопрос на уточнение, а не повод удалить блок**.
4. Если анализ ТЗ говорит, что обязательного блока быть не может (нет туториалов, нет покупок, событие не серверное) — **не выбрасывать молча**. Собрать такие блоки в отдельный список и задать вопрос до того, как отдавать файл на ревью. Формат вопроса — в конце этого файла.
5. Префикс `ХХ.` в шаблоне заменить на код фичи: `IDL.`, `CAH.`. Блок 38 идёт с префиксом `ХХ+1.` — так в шаблоне помечена задача следующей итерации.

---

## Планирование

### 1. Создание тест-плана — обязательный

```
На этапе планирования выделить ключевые точки и даты на задачи для входа в тестирование.
- Выбрать из шаблона декомпозиции необходимые проверки и добавить их в секцию QA проекта фичи
- Добавить специфичные проверки фичи
- Проставить поле <проект>: Проверка в QA
- Проставить зависимости от компонентов в разработке
- Проставить QA-сложность/QA Estimate
- Определить ключевые и наиболее багоопасные задачи из декомпозиции
- Определить конечные ДД отталкиваясь от дат выпуска фичи/регрессии на основании QA Estimate
```

Дополнить строкой с ключевыми точками готовности компонентов этой фичи.

### 2. Запросить читы для тестового окружения — обязательный

Исходная формулировка шаблона: «Необходимо на основе ТЗ проанализировать, какие читы будут необходимы для комфортного тестирования. Отдельно подумать над необходимостью читов на проде.»

**Здесь общей формулировки недостаточно — нужен список.** Вывести его из механики: всё, что игрок получает долго или случайно, должно выдаваться читом. Типовые категории: установка уровня/прогресса, сдвиг времени и принудительный старт/финиш, выдача каждой валюты фичи, спавн сущностей нужного грейда, телепорт по этапам и скип этапа, сброс просмотренных туториалов, сброс флага «видел фичу ранее», управление очередью промо-окон, переключение сложности.

### 37. Переэстимация фичи при глобальных изменениях ТЗ — обязательный

```
В случае глобальных изменений, переэстимировать время на фичу
```

---

## Запуск и инфраструктура

### 3. Проверка запуска события — обязательный

«Проверить старт события при различных условиях, в том числе на сохранении для проверки релиза.»

Наполнить условиями запуска из ТЗ: порог уровня, расписание, ограничения по остатку времени, первый и повторный запуск, приоритеты промо-окон.

### 4. Проверка лаунчера события — условный: есть лаунчер

```
Проверить:
- Даты и время события в игре соответствуют указанным в настройках конфига
- Лаунчер отрабатывает при выключенном кондишене "false"
- Завершение лаунчера через <finish> и <false/>
- Изменение дат события
```

Вложенная подзадача шаблона: **«Проверка реакции события при удалении лаунчера»** — проверка на QA Sandbox с удалением лаунчера события.

### 5. Проверка паков события — условный: у фичи есть свой пак ресурсов

```
В начале разработки уточнить, должен ли пак быть OnDemand (монтироваться по требованию) либо включён в автозагрузку.

Проверить:
- Если пак OnDemand — в base/PacksManager.xml прописано mountMode="OnDemand"; если пак в автозагрузке — условие отсутствует
- Паки скачиваются с заданного уровня
- Паки скачиваются при заниженной скорости интернета
- Событие не стартует без скачанных паков
- Паки скачиваются при включении интернета
- Не появляется окно дозагрузки ресурсов при старте скачивания паков
- Событие стартует при дозагрузке паков (если другие условия для запуска выполнены)
```

Дополнить составом пака: сколько локаций, биомов, вариантов оформления должно в него войти.

### 6. Проверка завершения события — обязательный

«Проверить завершение события при различных условиях, а также начисление и выдачу наград в различных условиях.»

Если в ТЗ несколько сценариев завершения с разными условиями входа — **разбить на отдельные блоки** (по прогрессу, по таймеру, бонусный этап). Так сделано в эталонной декомпозиции IDL.

### 10. Проверка работы без интернета — обязательный

В шаблоне описание пустое. Наполнить: запуск с ранее скачанным паком и последними загруженными настройками, поведение при обрыве связи посреди флоу, выдача наград и доход аналитики после восстановления сети.

---

## Механика фичи

Между блоками 6 и 7 вставляются **функциональные блоки, выведенные из ТЗ** — они у каждой фичи свои и в шаблоне отсутствуют. Правила их нарезки — в [SKILL.md](../SKILL.md), раздел «Как резать ТЗ на блоки».

### 7. Проверка туториалов события — условный: в фиче есть туториалы

```
Обратить внимание на:
- возобновление туториалов при прерывании
- пересечение туториалов с туториалами других событий
- отображение на разных разрешениях экранов
```

Дополнить перечнем туториалов фичи с их условиями запуска и признаком жёсткий/мягкий.

### 8. Проверка работы A/B-теста — обязательный

```
Проверить успешный запуск и завершение теста при разных условиях.
Настроить черновой тест для проверки должен программист.
```

Дописать, какие параметры фичи — кандидаты в A/B, или зафиксировать, что тест не планируется.

### 19. Проверка пересечения с другими событиями — обязательный

«Проверить возможные пересечения с другими событиями/фичами.»

Наполнить: конкретные конфликтующие события, поведение при завершении на их фоне, правила показа туториалов при пересечении, начисление ресурсов фичи из других режимов.

### 20. Проверка баунса при нажатии на кнопки интерфейса — обязательный

«Кнопки интерфейса реагируют на тап. Обычно — это стандартная анимация баунса.»

Строкой ниже перечислить все кнопки фичи, иначе проверка невыполнима.

---

## Данные, сервер, сохранки

### 12. Проверка синхронизации и обновления — обязательный

```
Проверить ситуации:
- Прогресс по событию сохраняется при синхронизации устройств
- Прогресс события сохраняется при переустановке приложения
- Прогресс события сохраняется после обновления игры
- Таймер события работает без прерывания и остановок
- Синхронизация во время активных туториалов
- Обновление и довыдача наград после обновления и синхронизации при различном прогрессе
Синхронизацию проверить по google, facebook, appleid, game center, google play games.
```

Дополнить перечнем того, что именно в этой фиче должно переживать синхронизацию: счётчики, прогресс разрушения, накопления, состояния особых режимов.

### 18. Проверка подхвата изменений с сервера — обязательный

«Проверить подхват изменений с сервера, в том числе в разных точках события, находясь в игре.»

### 23. Проверка отправки запросов на сервер / клиент-серверное взаимодействие — условный: серверное событие

В шаблоне описание пустое. Наполнить составом запросов, поведением при таймаутах и ошибках.

### 22. Проверка работы фичи на Prod-сервере — условный: **только для серверных событий** (прямая пометка в шаблоне)

```
Попросить Support Engineer настроить запуск лаунчера события на Prod-билде и проверить общую работоспособность.
Если серверное событие запускается в мире впервые — перед запуском сообщить серверным программистам для поднятия мощности серверов.
```

### 24. Проверка архивирования события в сохранке / задача от Support — обязательный

```
Проверить корректность архивирования информации о событии в сохранке.
Проверить логи на наличие ошибок. Поиск по словам "not found", "fail".
```

### 25. Проверка работы фичи для игрока с меткой жёсткого читера и тайм-читера — обязательный

Описание в шаблоне пустое. Наполнить тем, что в этой фиче зависит от времени и потому ломается тайм-читом.

---

## Кросс-функциональные

### 9. Проверка аналитики — обязательный

Ссылка на таблицу событий аналитики: `https://docs.google.com/spreadsheets/d/1ZhQCuEzx1d1Sj8lbCiJ5lBX5GUqOvpdiHrX3BsVeYRc/edit#gid=0`

Плюс из шаблона: «Проверить работу фичи без интернета, в том числе выдачу наград и приход аналитики после подключения к сети.» Если у фичи своя таблица аналитики и своя задача у аналитиков — вставить обе ссылки вместо переписывания событий.

### 11. Проверка нотификаций — условный: у фичи есть пуши

Описание в шаблоне пустое. Наполнить полным перечнем пушей с точным временем отправки и условиями; проверить приход в заявленное время, отсутствие при выключенных пушах, переход по тапу в нужную точку.

### 21. Проверка текстов и переводов — обязательный

Ссылка на таблицу текстов: `https://docs.google.com/spreadsheets/d/1NpTJJpn45jgNoW_Dk-R6zm_x7ZfKiqmio9wdG2RYRA0/edit?usp=sharing`

Дополнить: отсутствие плейсхолдеров и ключей локализации, переполнения в узких местах фичи (баблы, кнопки, плашки, тултипы) — перечислить эти места.

### 34. Проверка звуков и хаптика — обязательный

Описание в шаблоне пустое. Наполнить: джинглы старта и завершения, саундтрек и эмбиент, озвучка анимаций объектов, UI-звуки, особые звуки наград.

### 17. Проверка покупок по фиче на Inapp-билде — условный: в фиче есть покупки

```
Обязательно проверить, верно ли засчитывается кэш (если таковой имеется) — в реальный или внутриигровой. Посмотреть можно в читах.
```

---

## Платформы и устройства

### 13. Проверка фичи на low-устройствах и разрешениях экранов — обязательный

Описание в шаблоне пустое. **Обязательно добавить стресс-сценарий фичи**: что именно может просадить перф — количество объектов на сцене, множители скорости, VFX, память при переходах между локациями, вёрстка на notch и вытянутых экранах.

### 14. Проверка фичи на разных платформах — обязательный

```
Проверить работу фичи на всех платформах, включая китайский билд:
основное флоу, запуск, ресурсы и графику, платформозависимые фичи, работу на последней версии ОС.
```

В шаблоне у блока **семь вложенных подзадач** — по одной на платформу: android, amazon, samsung, huawei, mac, UWP, iOs CN. Воспроизводить их вложенными подзадачами, а не пунктами описания.

### 15. Проверка фичи на разных версиях iOS — обязательный

«Проверить на минимальной и максимальной версии iOS.»

### 16. Проверка работы фичи на ASAN-билде — обязательный

Описание в шаблоне пустое.

---

## Просмотры и стабилизация

Восемь блоков, которые легко забыть, — в эталонной декомпозиции IDL их нет, но в шаблоне они есть.

### 26–29. Просмотр. Прогон билда перед просмотром-1…4 — обязательные

```
Согласовать даты просмотра с ПМом и проставить в ДД задачи
Прогнать билд на поиск критичных багов в соответствии с продуктовым запросом.
```

Количество просмотров — по числу запланированных для фичи; в шаблоне заложено четыре.

### 30–33. Просмотр. Разбор фидбэка с просмотра-1…4 — обязательные

```
Создать задачи на замечания с просмотра и прикрепить ссылки в документ/задачу в соответствии с процессом в команде.
- При необходимости добавить шаги в описание
```

### 35. Стабилизация билда перед мержом — обязательный

```
Acceptance criteria. Протестированы:
- все компоненты
- все интеграции
- вмержены изменения из основной ветки
- допустимы дальнейшие визуальные правки и логика, не затрагивающая интеграционную логику
```

### 36. Проверка фичи после мерджа в мастер — обязательный

```
Согласовать с разработкой необходимость проверок после мерджа в мастер, в случае конфликтов провести проверки.
```

### 38. Чек-лист на регрессию в Qase и ТК на автоматизацию — обязательный

Идёт с префиксом `ХХ+1.` — следующая итерация после выпуска.

```
- Расписать шаги для тест-кейсов с полем Mast Pass в соответствии с инструкцией
- Отметить пункты для автоматизации чек-боксом To be automated
- Актуализировать в случае изменений
```

---

## Вложенная подзадача «Написание чек-листа»

В шаблоне у 13 блоков есть одинаковая вложенная подзадача:

```
ХХ. Написание чек-листа

Создать в Qase раздел с названием пункта декомпозиции
Внутри раздела через quick test добавить пункты проверки для данного модуля
Вставить в задачу ссылку на полученный чек-лист
```

Она висит у: запуска, лаунчера, паков, завершения, туториалов, аналитики, работы без интернета, нотификаций, синхронизации, пересечения событий, Prod-сервера, клиент-серверного взаимодействия, читеров, звуков.

Правило: **вешать её на каждый блок, где проверок много и они пойдут в регресс.** В файле декомпозиции помечать строкой `Вложенная подзадача: Написание чек-листа` в конце блока — при выгрузке в Asana она создаётся через `asana_create_subtask` с родителем-блоком.

---

## Если блок не применим

Не удалять молча. Собрать все спорные блоки и задать вопрос **до** отдачи файла на ревью, одним списком:

```
По ТЗ не вижу оснований для этих обязательных разделов — подтвердите, убирать или наполнять:

- Проверка туториалов — в ТЗ раздела про обучение нет. Туториалы точно не планируются?
- Проверка покупок на Inapp-билде — в ТЗ нет ни одной точки покупки за реальные деньги. Верно?
- Проверка работы на Prod-сервере — в шаблоне помечена как актуальная только для серверных событий. Событие клиентское?
- Проверка нотификаций — в ТЗ пушей нет. Их не будет или раздел ТЗ ещё не написан?

Просмотров в плане — сколько? В шаблоне заложено четыре, оставил столько же.
```

Последняя формулировка важна: **отсутствие раздела в ТЗ и отсутствие функциональности — разные вещи.** Первое чаще означает, что ТЗ недописано, и тогда это `[GAP-xx]`, а не удаление блока.

Чего в этом списке быть не должно: **вопросов про версию.** Целевую версию задач QA проставляет сам, декомпозиция её не касается. Если пометка в ТЗ ограничивает объём функциональности, это `[GAP-xx]` про объём релиза, а не вопрос о номере версии.
